<%= form_for(@salary_item) do |f| %>
  <% if @salary_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@salary_item.errors.count, "error") %> prohibited this salary_item from being saved:</h2>

      <ul>
      <% @salary_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
